{
  "name": "skejjo-dev",
  "version": "1.0.0",
  "description": "SKEJJO Consulting website",
  "license": "UNLICENSED",
  "scripts": {
    "dev": "hugo server",
    "build": "hugo --gc --minify",
    "build:pagefind": "hugo --gc --minify && npx pagefind --site public"
  },
  "devDependencies": {
    "@fullhuman/postcss-purgecss": "^5.0.0",
    "pagefind": "^1.3.0",
    "postcss": "^8.4.20",
    "postcss-cli": "^10.1.0"
  }
}
